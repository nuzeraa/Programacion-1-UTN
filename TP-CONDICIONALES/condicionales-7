# Solicitar una frase o palabra al usuario
texto = input("Por favor, ingresar una frase o palabra: ")

# Verificar si el último carácter es una vocal (mayúscula o minúscula)
if texto[-1].lower() in 'aeiou':
    texto += "!"

# Imprimir el resultado final
print("Resultado:", texto)