# Solicitar al usuario los dos valores
inicio = int(input("Introduce el primer número: "))
fin = int(input("Introduce el segundo número: "))

# Asegurar que 'inicio' sea menor que 'fin'
if inicio > fin:
    inicio, fin = fin, inicio

# Calcular la suma de los números comprendidos entre ellos, excluyendo los límites
suma = 0
for i in range(inicio + 1, fin):
    suma += i

# Mostrar el resultado
print(f"La suma de los enteros entre {inicio} y {fin}, excluyéndolos, es: {suma}")